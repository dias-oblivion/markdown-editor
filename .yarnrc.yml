# Usa node_modules (com hoisting) em vez de Plug'n'Play.
# Evita a rigidez do PnP que bloqueia imports transitivos e quebra `npx tsc`.
nodeLinker: node-modules

# Permite postinstall scripts (Yarn 4 desabilita por padrao).
# Necessario para o Electron baixar seu binario.
enableScripts: true
